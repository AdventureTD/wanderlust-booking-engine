# S13 prerequisite — explicit finite source admission

## Authorization and provenance decision, before backend execution

This is a NEW ordinary deterministic fixture assignment, not the earlier preserve-source recovery. The current user expressly permits a separately frozen actual-issuer test vector after source admission and independent oracle crosscheck. The original issuer admission is `booking-guest-offer-acceptance-implementation.md:3–8`: exact ordinary issueGuestBookingOffer/acceptGuestBookingOffer calls, native crypto, inert SDK; producer/default/restricted inventories excluded. Original assignment confirmed in session `20260908_223834_7b0a08`, message 91349. Original status assignment is message 96110. The production skill additionally requires deterministic original-payload/encoded/MAC/full-token golden values independently checked. None of these records identifies a recovered credential golden file. The public quote vector is expressly NOT a guest credential golden (`booking-guest-summary-connector-independent-review.md:27`).

The current assignment supplies the missing new-fixture scope. No new business authorization is needed. It does not transfer any completion-producer/restricted-suite approval. Prior status admission forbade issuer invocation in its reader loader; that remains unchanged. The NEW issuer-only runner below is a separate, finite admission and does not use that loader to issue anything.

## Exact admitted execution

Ownroot: `C:/Users/TomDe/checkpoints/booking-guest-status-final-review-evidence/ownroot`.
Parent: `bdb4093bb4332594001436b50eb47b9c4e693761`.
`custody-before.json` records exact sources, fixture bytes, raw/canonical dependency hashes and every admitted issuer import. All 29 inherited status dependencies match exact canonical parent blobs. All four status authored files match saved S12 hashes. No original file is edited.

1. Execute ONLY new-vector-generator.cjs, raw SHA-256 `d635b33b3b611c92f684a009f912c6b6ff2d2104ebdc5ecd45ddd7d026b508c9`.
2. Its closed nine-module graph: OfferIssuer, PurchaseInput, PriceGroups, FinancialCalculation, lockedPricingQuoteAuthority, strictLockedPricingQuote, IssuerAuthority, Credentials, AccessPolicy. Native Node HMAC/hash/timing-safe comparison/Buffer; deterministic public test entropy replaces only randomBytes at the inert boundary. Exactly two calls: 24 bytes for booking suffix, 32 bytes for intentId. These NEW chosen fixture values come from the public retained record; they are NOT recovered historical RNG output and prove no live entropy property.
3. Reconstruct only ordinary purchase input from the untouched retained capsule's inputCanonical. Preserve original signed quote bytes, revision bytes/config, keyring and issued time. Original quote HMAC independently verified in Python using its original documented PUBLIC-READER-QUOTE-FIXTURE-ONLY-KEY (harness read solely for schema/public-key provenance, never loaded or executed).
4. Only inert financial-revision read and three named public fixture secret values. SDK writes and unlisted imports denied. NO Acceptance invocation, retained row creation, discovery, handoff, physical acquisition, completion producer, recovery coordinator, provider or socket capability.
5. Actual issueGuestBookingOffer must return the exact retained capsule; actual credential service attenuates and verifies both status tokens. Output is NEW public test data, not recovered authority.
6. Crosscheck both complete output tokens, exact canonical ASCII eight-tuple payload, base64url and domain-separated HMAC against separately frozen `independent-python-oracle.json`, SHA-256 `ab58ce2bd06f7a90d536f915f4a3e4d9e110f4aee5648cac9c94b9f43a0713c6`. Freeze matched output as NEW credential-vector.json only in ownroot.
7. Only after that match, execute `node scripts/guest-status/status-tests.cjs S13` in ownroot, unchanged original runner/loader/runtime. Before invocation compare all source/fixture hashes and record vector hash. No S01–S12 rerun. Status loader keeps RNG/write denial traps and unchanged source graph. S13 result must have native exit 0 and exactly completed=1,cases=[S13].

## Original vector format

S13 reads only JSON string fields bootstrap/access, compares both byte-for-byte with its public fixture envelope and actual attenuation, then requires both authenticated reads to return only CONFIRMED and own bookingNumber with unchanged retained rows. Credential envelope is `wgb1.<kid>.<payload-base64url>.<mac-base64url>`; payload canonical ASCII JSON `[1,purpose,audience,intentId,intentDigest,quoteDigest,issuedAtMs,expiresAtMs]`; MAC HMAC-SHA256 over UTF-8 `WBE-GUEST-BOOKING-CREDENTIAL` + NUL + envelope without MAC, using the original public 32-byte hex key. Purpose differs bootstrap/access; subject/time/offer binding does not. No token normalization, repair or authority seeding is admitted.

Independent review of the preexisting adapter is separate from authorship of this new fixture support; this document admits bounded execution, not commit/public/live eligibility or independent approval of its own newly authored runner.
