'use strict';
const fs=require('node:fs'),vm=require('node:vm');let source=fs.readFileSync(__dirname+'/verify-guest-booking-allocation-evidence.js','utf8');const i=source.indexOf("test('inert SDK");if(i<0)throw Error('fixture boundary');source=source.slice(0,i)+String.raw`
test('C15 actual adapter normalization released reuse and partial overlap',async()=>{
 for(const mode of ['released-reuse','partial-overlap','partial-outside']){const {s,accepted}=await setup(),core=s.load('backend/roomBookingCommitRules'),outside=mode==='partial-outside',ci=outside?'2026-01-01':'2027-01-01',co=outside?'2026-01-02':'2027-01-03',snapshot={occupiedUnits:[],occupiedUnitsByNight:outside?{'2026-01-01':[]}:{'2027-01-01':[],'2027-01-02':[]},migrationIssueRows:[],duplicateUnitClaims:[],unknownStatusRows:[]},request={operationId:'historyoperation0001',bookingNumber:'HISTORY',payloadDigest:'1'.repeat(64),checkIn:ci,checkOut:co,roomCode:'adventure_suite',quantity:1},old=core.buildPhysicalCommitPlan(s.realm(snapshot),s.realm([]),s.realm(request)),op=old.acquisitions[0];
 let ledger=plain(old.acquisitions.slice(0,2));
 if(mode==='released-reuse'){const complete={_id:'rc1-op-'+op.operationId+'-c',protocolVersion:1,claimKey:'operation:'+op.operationId+':completion',generation:1,eventType:'complete',claimType:'operation-completion',operationId:op.operationId,bookingRowId:op.bookingRowId,bookingNumber:op.bookingNumber,payloadDigest:op.payloadDigest,completionState:'complete',confirmedResourceCount:old.acquisitions.length-1,decisionFenceVersion:1},decision={_id:'rc1-op-'+op.operationId+'-d',protocolVersion:1,claimKey:'operation:'+op.operationId+':decision',generation:1,eventType:'decide',claimType:'operation-decision',operationId:op.operationId,bookingRowId:op.bookingRowId,bookingNumber:op.bookingNumber,payloadDigest:op.payloadDigest,decisionFenceVersion:1,operationIdentityId:op._id,operationCompletionId:complete._id,manifestVersion:1,completionState:'complete',confirmedResourceCount:complete.confirmedResourceCount,decisionState:'compensate'};ledger=plain([...old.acquisitions,complete,decision,...core.planPhysicalRollback(old.acquisitions,'Synthetic cancellation')]);}
 s.state.db.rows.RoomBookingClaimEvents=ledger;const read=await s.load('backend/guestBookingAllocationEvidence').readGuestBookingAllocationEvidence('2027-01-01','2027-01-03');assert.equal(read.status,'READY',mode);const sorted=ledger.slice().sort((a,b)=>a._id<b._id?-1:1);assert.deepEqual(plain(read.claimLedger),sorted);assert.deepEqual(plain(read.planningEvidence[3]),sorted.map(r=>[r._id,null,null,null]));
 const result=await handoff(s,accepted);assert.equal(result.status,mode==='partial-overlap'?'ALLOCATION_PENDING':'ALLOCATION_HANDOFF_PENDING',mode);assert.deepEqual(s.state.db.rows.GuestBookingAcceptances,[accepted]);if(mode==='partial-overlap'){assert.equal(result.reason,'UNSUPPORTED_PLAN');noInsert(s);}else{const row=plain(s.state.db.rows.GuestBookingAllocationManifests[0]),t=JSON.parse(row.manifestCanonical);assert.deepEqual(t[9][2],sorted);assert.equal(t[3][6],accepted.capsule);assert.deepEqual(JSON.parse(t[3][6]).calculation,JSON.parse(accepted.capsule).calculation);if(mode==='released-reuse')assert.ok(t[6][0].acquisitions.filter(r=>r.claimType!=='operation').some(r=>r.generation===2));const x=subject(s.state.db);x.context.clock=()=>{throw Error('clock');};x.load('backend/wholeCartPlanningRules').buildWholeCartAllocation=()=>{throw Error('replan');};const query=x.wix.query;x.wix.query=c=>{assert.ok(['GuestBookingAcceptances','GuestBookingAllocationManifests'].includes(c));return query(c);};assert.equal((await handoff(x,accepted)).status,'ALLOCATION_HANDOFF_PENDING');assert.deepEqual(s.state.db.rows.GuestBookingAllocationManifests,[row]);assert.ok(x.state.trace.every(t=>t.op==='find'));}console.log('C15 '+mode+' '+result.status);
 }
});
(async()=>{let failures=0;for(const [name,fn]of tests){try{await fn();console.log('PASS '+name);}catch(e){failures++;console.error('FAIL '+name,e);}}if(failures)process.exitCode=1;})();
`+'\n`,context);';vm.runInNewContext(source,{require,Buffer,console,process,__dirname});
